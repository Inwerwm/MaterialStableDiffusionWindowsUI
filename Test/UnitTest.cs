using MaterialStableDiffusionWindowsUI.Core.Models;

namespace Test;

public class Tests
{
    [SetUp]
    public void Setup()
    {
    }

    [Test]
    public async Task GenerationTest()
    {
        //var client = new GeneratorClient();
        //var outputs = await client.Generate(new() { 
        //    Prompt = "Elegant red cushion",
        //    Width = 512,
        //    Height = 512,
        //    GuidanceScale = 7.5f,
        //    NumOutputs = 1
        //});

        //foreach (var output in outputs)
        //{
        //    Console.WriteLine(output);
        //    Convert.FromBase64String(output.Substring(22));
        //}

    }
}